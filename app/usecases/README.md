# Usecases

Purpose

- Encapsulates application business logic as composable use-cases.

Main components

- Individual use-case modules such as `patient.py`, `patient_note.py`, `patient_summary.py`.

Responsibilities

- Orchestrate domain models, repositories, and services to fulfill application operations.
- Keep controllers thin by centralizing business rules in this layer.
