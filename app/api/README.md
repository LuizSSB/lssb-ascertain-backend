# API layer

Purpose

- Declare endpoints/routes provided by and middleware used by FastAPI.

Main components

- `middleware/` - HTTP middleware, such as request logging.
- `routes` - the endpoints and the functions to be run at each one.

Responsibilities

- Handle HTTP requests made to the API (processing, tracking, etc).
