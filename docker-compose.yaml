services:
  api:
    build: .
    container_name: lssb_ascertain_api
    ports:
      - "8000:8000"
    volumes:
      - ./app:/app
    environment:
      - ENV=development
    restart: always
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
